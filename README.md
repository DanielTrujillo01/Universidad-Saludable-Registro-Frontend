
  # UVS-REGISTRO

  This is a code bundle for UVS-REGISTRO. The original project is available at https://www.figma.com/design/7e7PR9eS8njkK11Nj4Kq14/UVS-REGISTRO.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  